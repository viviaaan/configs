# config to download songs

--extract-audio

--add-metadata

--embed-thumbnail

--restrict-filenames

--format "bestaudio[ext=mp3]/bestaudio[ext!=opus]/best[ext!=opus]"

--audio-format mp3

--output ~/music/%(track)s-%(artist)s.%(ext)s
